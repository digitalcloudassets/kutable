import React from 'react';

export default function AIChatWidget() {
  if (import.meta.env.DEV) {
    // eslint-disable-next-line no-console
    console.warn('[AIChatWidget] Removed from product. This is a no-op placeholder.');
  }
  return null;
}